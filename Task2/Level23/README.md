# Vint Cerf Lab - Protocol Selector

Open `index.html` in a browser to view the interactive page. Click a protocol (TCP/HTTP/SMTP/FTP) then click a card to assign it. Correct assignments get a green highlight.
